@page
@model SportEvents.WebApplication.Pages.Participants.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Participant</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="@Model.Item.Id" />
            <div class="form-group">
                <label asp-for="@Model.Item.Name" class="control-label"></label>
                <input asp-for="@Model.Item.Name" class="form-control" />
                <span asp-validation-for="@Model.Item.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.Item.Email" class="control-label"></label>
                <input asp-for="@Model.Item.Email" class="form-control" />
                <span asp-validation-for="@Model.Item.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="@Model.Item.PhoneNumber" class="control-label"></label>
                <input asp-for="@Model.Item.PhoneNumber" class="form-control" />
                <span asp-validation-for="@Model.Item.PhoneNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
